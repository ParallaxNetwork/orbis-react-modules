{
  "name": "orbis-react-modules",
  "version": "0.0.1",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "test"
    ]
  },
  "scripts": {
    "dev": "npm --prefix ./test/ run dev",
    "build": "npm --prefix ./test/ run build --output public",
    "preview": "npm --prefix ./test/ run preview"
  }
}
