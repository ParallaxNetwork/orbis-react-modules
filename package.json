{
  "name": "orbis-react-modules-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*",
    "test"
  ],
  "scripts": {
    "buil": "npm --prefix ./test/ run build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
